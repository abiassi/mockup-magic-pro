{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "vite --port $PORT",
  "installCommand": "npm install",
  "framework": "vite",
  "functions": {
    "api/upscale.ts": { "maxDuration": 60 },
    "api/mcp.ts": { "maxDuration": 60 }
  }
}

